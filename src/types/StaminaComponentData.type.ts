export type StaminaComponentDataType = {
  maxStamina?: number; // Maximum stamina points (default: 100)
  regenRate?: number; // Stamina regenerated per second (default: 20)
  regenDelay?: number; // Seconds after spending before regen starts (default: 1.0)
};
