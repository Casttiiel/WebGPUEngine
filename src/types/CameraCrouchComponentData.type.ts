export type CameraCrouchComponentDataType = {
  slideCrouchHeight?: number; // Altura de la cámara durante slide (metros)
  crouchSpeed?: number; // Velocidad de interpolación
  enabled?: boolean; // Activar/desactivar el efecto
};
