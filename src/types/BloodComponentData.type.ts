export type BloodComponentDataType = {
  maxBlood?: number; // Maximum blood points (default: 100)
  regenRate?: number; // Blood regenerated per second (default: 15)
  regenDelay?: number; // Seconds after last use before regen starts (default: 10.0)
};
