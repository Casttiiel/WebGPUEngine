/**
 * Identificadores únicos de cada habilidad mágica del juego.
 * Se usan como clave en el registro de desbloqueados del AbilitySystem.
 */
export enum AbilityId {
  LEVITATION = 'levitation',
  PUSH = 'push',
  SPAWN_CHAIN = 'spawn_chain',
}
