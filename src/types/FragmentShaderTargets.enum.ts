export enum FragmentShaderTargets {
  SCREEN = 'screen',
  GBUFFER = 'gbuffer',
  PARTIAL_GBUFFER = 'partial_gbuffer',
  TEXTURE = 'texture',
  DEPTH_ONLY = 'depth_only',
  SINGLE_CHANNEL = 'single_channel',
  OIT_GATHER = 'oit_gather',
  MASK = 'mask',
}
